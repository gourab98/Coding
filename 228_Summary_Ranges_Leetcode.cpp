#include<bits/stdc++.h>
using namespace std;
int main()
{
   vector<int> vec;
   int input;
   while(cin>>input){
       vec.push_back(input);
   }

   vector<string> ans;
   int left = 0, n = vec.size();
   for(int i=0;i<n;i++){
       if(i<n-1 && vec[i] == vec[i+1]-1) continue;

       if(i==left){
                        
       }
       else {

       }
   }
}